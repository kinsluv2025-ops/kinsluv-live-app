# Kinsluv Live – Full Repo Skeleton (Extended)

This repository is a ready-to-deploy skeleton for a Kinsluv-style live streaming app
using ZEGOCLOUD (Zego) for real-time streaming, MongoDB for persistence, React frontend,
Node.js backend, Docker, and GitHub Actions CI.

This extended package includes:
- Frontend (React) with Zego prebuilt UI placeholders
- Backend (Node.js/Express) with token generator placeholder (REPLACE with Zego official generator)
- MongoDB schemas and simple session endpoints
- Dockerfile & docker-compose for local dev
- Vercel (frontend) and Render (backend) deploy config examples
- Recording / co-host / gifts placeholders and notes
- GitHub Actions CI
- LICENSE (MIT)

> IMPORTANT: **Do not** store your ZEGOCLOUD server secret in client code or public repos.
Use environment variables / secret managers.

